# Julia
/Manifest.toml.bak
*.jl.cov
*.jl.*.cov
/deps/deps.jl
.CondaPkg/

# Python glue
__pycache__/
*.py[cod]
.venv/

# Secrets / local
.env
*.local

# Data / caches
*.parquet
data_cache/
.codegraph/

# OS
.DS_Store

# BlaqueBaux runtime — ledger, spine state, audit + run logs (never commit; may contain
# account/position data). Keys live in ~/.config/blaquebaux/alpaca.env, outside the repo.
logs/
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.sqlite-journal
*.jls
*_audit.jsonl
*audit*.jsonl
